= Implementation Backlog: Zitate Web v1.0
:toc:
:toclevels: 3

== Overview
This backlog contains user stories and implementation tasks for the web-based SPA version of Zitate, derived from the requirements specification. Stories are organized by epic and prioritized for iterative development.

== Epic 1: Core Data Layer and IndexedDB

=== Story 1.1: IndexedDB Schema Setup
*As a* developer +
*I want to* create the IndexedDB schema with all required object stores +
*So that* data can be stored and retrieved from the browser

.*Acceptance Criteria:*
- Database "zitate-db" initialized on first app load
- Object stores: entries, authors, labels, images, audio, folders
- Indexes created for frequently queried fields
- Schema version 1 defined
- Migration system in place for future versions

.*Reference:*
FR-1.1, FR-1.2, NFR-2

.*Technical Tasks:*
1. Create db/schema.ts with object store definitions
2. Implement database initialization in db.service.ts
3. Add indexes (createdAt, name, entryId, etc.)
4. Create migration system (version-based upgrades)
5. Add error handling for quota exceeded
6. Write unit tests with fake-indexeddb

.*Estimated Effort:*
6 hours

'''

=== Story 1.2: TypeScript Interfaces
*As a* developer +
*I want to* define TypeScript interfaces for all data models +
*So that* the codebase has type safety

.*Acceptance Criteria:*
- Entry, Author, Label, ImageAttachment, AudioAttachment, SmartFolder interfaces
- FolderCriteria nested interface
- Validation types (ValidationResult)
- Export all types from models/index.ts

.*Reference:*
All FR requirements

.*Technical Tasks:*
1. Create models/Entry.ts interface
2. Create models/Author.ts interface
3. Create models/Label.ts interface
4. Create models/ImageAttachment.ts interface
5. Create models/AudioAttachment.ts interface
6. Create models/SmartFolder.ts interface
7. Export all from models/index.ts

.*Estimated Effort:*
3 hours

'''

=== Story 1.3: IndexedDB CRUD Service
*As a* developer +
*I want to* create a service wrapper for IndexedDB operations +
*So that* components can easily interact with the database

.*Acceptance Criteria:*
- DBService class with methods: get, getAll, add, update, delete, query
- Transaction management (readonly vs readwrite)
- Error handling and logging
- Promise-based API
- Cursor support for pagination

.*Reference:*
NFR-2

.*Technical Tasks:*
1. Create services/db.service.ts
2. Implement init() method
3. Implement CRUD methods
4. Add query method with index support
5. Add cursor-based pagination
6. Add error handling
7. Write unit tests

.*Estimated Effort:*
8 hours

'''

=== Story 1.4: Data Validation Utilities
*As a* developer +
*I want to* create validation functions for all data types +
*So that* invalid data is prevented from being stored

.*Acceptance Criteria:*
- validateEntryText (1-10,000 chars)
- validateAuthorName (max 200 chars)
- validateLabelName (max 50 chars, no comma/semicolon)
- validateURL format
- validateFolderName (max 100 chars)
- ValidationResult type for consistent error reporting

.*Reference:*
FR-1.1, FR-2.2, FR-3.3

.*Technical Tasks:*
1. Create utils/validators.ts
2. Implement text validation
3. Implement name validations
4. Implement URL validation
5. Create ValidationResult type
6. Write unit tests for all validators

.*Estimated Effort:*
3 hours

'''

== Epic 2: Entry Management (Basic)

=== Story 2.1: Entry Creation Form Component
*As a* user +
*I want to* click a plus button to create a new entry +
*So that* I can add a new quote to my collection

.*Acceptance Criteria:*
- Plus button visible on home screen (header or FAB)
- Clicking opens modal or navigates to entry form
- Form includes: text area, location display
- Text field is required
- Cancel and Save buttons

.*Reference:*
FR-1.5

.*Technical Tasks:*
1. Create components/entry/EntryForm.tsx
2. Create components/common/Modal.tsx (if modal approach)
3. Add form state management (useState or form library)
4. Add plus button to Header component
5. Implement open/close modal logic
6. Style form (mobile responsive)
7. Add keyboard navigation (Escape to close)

.*Estimated Effort:*
6 hours

'''

=== Story 2.2: Text Input with Validation
*As a* user +
*I want to* enter text for my quote +
*So that* I can record the saying or citation

.*Acceptance Criteria:*
- Multi-line textarea
- Character counter showing 0/10,000
- Validation error if empty or >10,000 characters
- Real-time validation as user types
- Visual feedback (color coding)

.*Reference:*
FR-1.1

.*Technical Tasks:*
1. Add textarea to EntryForm
2. Add character counter display
3. Implement onChange handler with validation
4. Add visual feedback (border color, error message)
5. Disable save button if invalid
6. Add debouncing for real-time validation
7. Test with various inputs (0 chars, 10000 chars, 10001 chars)

.*Estimated Effort:*
3 hours

'''

=== Story 2.3: Geolocation Integration
*As a* user +
*I want to* have my current location automatically captured +
*So that* I don't have to manually enter it

.*Acceptance Criteria:*
- Browser geolocation permission requested on form open
- GPS coordinates captured and displayed
- Address displayed if reverse geocoding available
- Fallback to coordinates if geocoding fails
- App works if permission denied (location optional)

.*Reference:*
FR-1.2

.*Technical Tasks:*
1. Create services/location.service.ts
2. Implement getCurrentPosition using Geolocation API
3. Handle permission request and denial
4. Optional: Implement reverse geocoding (OpenStreetMap Nominatim)
5. Create hooks/useLocation.ts hook
6. Integrate into EntryForm
7. Display coordinates or address
8. Test permission flows (granted, denied, prompt)

.*Estimated Effort:*
6 hours

'''

=== Story 2.4: Entry Save to IndexedDB
*As a* user +
*I want to* save my completed entry +
*So that* it's stored in my collection

.*Acceptance Criteria:*
- Save button creates entry in IndexedDB
- Entry appears immediately in lists/searches
- Form resets after save
- Modal closes or navigates away
- Error handling if save fails

.*Reference:*
FR-1.5

.*Technical Tasks:*
1. Create hooks/useEntries.ts hook
2. Implement addEntry function
3. Generate UUID for new entry
4. Store entry with timestamp
5. Call DBService.add('entries', entry)
6. Handle errors (quota exceeded, etc.)
7. Reset form on success
8. Close modal/navigate
9. Write integration tests

.*Estimated Effort:*
4 hours

'''

=== Story 2.5: Entry List Display
*As a* user +
*I want to* see my saved entries on the home screen +
*So that* I can browse my collection

.*Acceptance Criteria:*
- Entries displayed as cards
- Shows text preview (first 160 chars), date, location
- Sorted by creation date (newest first)
- Empty state when no entries
- Click card to view details (future story)

.*Reference:*
FR-5.2, FR-5.5

.*Technical Tasks:*
1. Create components/entry/EntryCard.tsx
2. Create components/entry/EntryList.tsx
3. Fetch entries from IndexedDB in useEffect
4. Map entries to EntryCard components
5. Implement empty state component
6. Add basic styling (card layout, spacing)
7. Test with 0 entries, 1 entry, many entries

.*Estimated Effort:*
5 hours

'''

=== Story 2.6: Entry Deletion
*As a* user +
*I want to* delete an entry +
*So that* I can remove quotes I no longer want

.*Acceptance Criteria:*
- Delete button visible on entry card (hover on desktop)
- Clicking shows confirmation dialog
- Confirmation shows entry preview
- Deletion removes from IndexedDB
- UI updates immediately

.*Reference:*
FR-1.6

.*Technical Tasks:*
1. Add delete button to EntryCard
2. Create components/common/ConfirmDialog.tsx
3. Implement deleteEntry in useEntries hook
4. Show confirmation before delete
5. Remove from IndexedDB
6. Update local state to re-render list
7. Add toast/notification for success
8. Test deletion flow

.*Estimated Effort:*
4 hours

'''

=== Story 2.7: Citation Date Input
*As a* user +
*I want to* set the date of the quote independently from the entry creation timestamp +
*So that* I can capture when the quote was originally said or published

.*Acceptance Criteria:*
- Entry form includes a citation date input
- Citation date defaults to the current entry creation date for new entries
- Citation date can be changed during entry creation
- Citation date can be edited later without changing `createdAt`
- Validation and save flow handle citation date correctly

.*Reference:*
FR-1.8

.*Technical Tasks:*
1. Extend `Entry` model with `citationDate`
2. Add citation date field to `EntryForm`
3. Default citation date to entry creation date for new entries
4. Update `useEntries` add/update logic to persist `citationDate`
5. Add tests for defaulting and editing behavior

.*Estimated Effort:*
4 hours

'''

=== Story 2.8: URL Attachment Input
*As a* user +
*I want to* attach one or more URLs to an entry +
*So that* I can keep source and reference links together with the quote

.*Acceptance Criteria:*
- Entry form supports adding multiple URLs
- Each URL is validated before save
- Individual URLs can be removed before saving
- Editing an entry allows adding and removing URLs
- Each attached URL stores its own attachment timestamp

.*Reference:*
FR-1.7

.*Technical Tasks:*
1. Define `EntryLink` type for URL metadata
2. Add URL list input UI to `EntryForm`
3. Reuse or extend URL validation utilities
4. Implement add/remove URL interactions
5. Generate metadata (`id`, attachment timestamp) for each URL
6. Add tests for valid, invalid, add, and remove flows

.*Estimated Effort:*
5 hours

'''

=== Story 2.9: Persist Citation Date and URLs
*As a* user +
*I want to* have citation dates and attached URLs saved with each entry +
*So that* this metadata is available across sessions and later edits

.*Acceptance Criteria:*
- IndexedDB entry records include `citationDate` and attached URLs
- Existing add/update entry flows persist the new fields
- Existing entry loading returns citation date and URLs correctly
- Schema migration or backward-compatible defaults handle older entries
- Older entries without the new fields remain readable

.*Reference:*
FR-1.7, FR-1.8

.*Technical Tasks:*
1. Extend IndexedDB `entries` schema with backward-compatible handling
2. Update `createEntry` defaults
3. Update `useEntries` serialization/deserialization
4. Add fallback logic for legacy entries missing the new fields
5. Add tests for persistence and backward compatibility

.*Estimated Effort:*
5 hours

'''

== Epic 3: UI Foundation and Responsive Design

=== Story 3.1: App Shell and Layout
*As a* user +
*I want to* have a consistent app layout +
*So that* navigation is intuitive

.*Acceptance Criteria:*
- Header with app name and actions
- Main content area
- Footer with links (optional)
- Responsive on mobile/tablet/desktop
- Navigation works on all screen sizes

.*Reference:*
FR-5.1

.*Technical Tasks:*
1. Create components/layout/Header.tsx
2. Create components/layout/Footer.tsx
3. Create components/layout/Layout.tsx wrapper
4. Add responsive CSS (mobile-first)
5. Implement CSS Grid for layout
6. Add logo/branding
7. Test on 320px, 768px, 1024px+ viewports

.*Estimated Effort:*
5 hours

'''

=== Story 3.2: Dark Mode Support
*As a* user +
*I want to* use dark mode +
*So that* the app is comfortable at night

.*Acceptance Criteria:*
- Detects system preference (prefers-color-scheme)
- CSS custom properties for colors
- All components styled for both modes
- Optional: Manual toggle (future)

.*Reference:*
FR-5.1

.*Technical Tasks:*
1. Define CSS custom properties in styles/variables.css
2. Create light theme values
3. Create dark theme values with @media (prefers-color-scheme: dark)
4. Update all components to use custom properties
5. Test in both modes
6. Ensure WCAG contrast ratios met

.*Estimated Effort:*
4 hours

'''

=== Story 3.3: Keyboard Navigation
*As a* user +
*I want to* navigate the app with keyboard +
*So that* I can use it without a mouse

.*Acceptance Criteria:*
- Tab order is logical
- Enter/Space activate buttons
- Escape closes modals
- Focus indicators visible
- No keyboard traps

.*Reference:*
FR-5.1 (WCAG 2.1 AA)

.*Technical Tasks:*
1. Set tabindex on interactive elements
2. Add keyboard event handlers
3. Implement focus management for modals
4. Add visible :focus styles
5. Test with Tab, Enter, Space, Escape keys
6. Test with screen reader (NVDA/JAWS/VoiceOver)

.*Estimated Effort:*
3 hours

'''

== Epic 4: Media Features

=== Story 4.1: Image Upload
*As a* user +
*I want to* attach images to my entries +
*So that* I can add visual context

.*Acceptance Criteria:*
- File input allows image selection
- Up to 10 images per entry
- Images compressed to max 2MB
- Thumbnails displayed
- Images stored as Blobs in IndexedDB

.*Reference:*
FR-1.3

.*Technical Tasks:*
1. Add file input to EntryForm
2. Create services/media.service.ts
3. Implement image compression (browser-image-compression library)
4. Store images in IndexedDB 'images' object store
5. Link images to entry via entryId
6. Display thumbnails in form
7. Test with various image formats (JPEG, PNG, HEIC)

.*Estimated Effort:*
6 hours

'''

=== Story 4.2: Image Deletion
*As a* user +
*I want to* remove individual images +
*So that* I can manage my attachments

.*Acceptance Criteria:*
- Delete button (X) on each thumbnail
- Clicking removes image from entry and IndexedDB
- Immediate UI update

.*Reference:*
FR-1.3

.*Technical Tasks:*
1. Add delete button to thumbnail component
2. Implement removeImage in useEntries hook
3. Delete from IndexedDB 'images' store
4. Remove from entry.imageIds array
5. Update UI state

.*Estimated Effort:*
2 hours

'''

=== Story 4.4: Image Management in Edit Mode – Add Images
*As a* user +
*I want to* add new images when editing an existing entry +
*So that* I can attach additional visual context later

.*Acceptance Criteria:*
- ImageUpload component is shown in edit mode (not only on creation)
- New images are compressed, stored in IndexedDB, and linked to the entry
- Entry's imageIds array is updated with the new IDs
- Max 10 images per entry limit is enforced (existing + new)
- Existing images remain visible alongside newly added ones

.*Reference:*
FR-1.3

.*Technical Tasks:*
1. Remove `!isEditing` guard on ImageUpload in EntryForm
2. Create ImageEditor component combining existing images + upload
3. Extend updateEntry in useEntries to accept new images (SelectedImage[])
4. Save new images via saveImages, append IDs to entry.imageIds
5. Pass existing image count to ImageUpload for limit enforcement
6. Update App.tsx handleSaveEntry to forward selectedImages on edit

.*Estimated Effort:*
4 hours

'''

=== Story 4.5: Image Management in Edit Mode – Delete Images
*As a* user +
*I want to* remove existing images when editing an entry +
*So that* I can clean up unwanted attachments

.*Acceptance Criteria:*
- Each existing image thumbnail shows a delete (×) button in edit mode
- Deleting marks the image for removal (applied on save)
- Deleted images are removed from IndexedDB and entry.imageIds on save
- Visual feedback: deleted thumbnails shown with strikethrough/dimmed or removed immediately

.*Reference:*
FR-1.3

.*Technical Tasks:*
1. Track imagesToDelete state in EntryForm
2. Show existing images in ImageEditor with delete buttons
3. Extend updateEntry to accept imagesToDelete: string[]
4. On save: delete images from IndexedDB, remove IDs from entry.imageIds
5. Update remaining image order fields

.*Estimated Effort:*
3 hours

'''

=== Story 4.6: Image Management in Edit Mode – Reorder Images
*As a* user +
*I want to* reorder images when editing an entry +
*So that* the most important image appears first

.*Acceptance Criteria:*
- Move-up / move-down buttons on each image thumbnail in edit mode
- Reordering updates the display immediately
- On save, the new order is persisted (imageIds order + ImageAttachment.order)

.*Reference:*
FR-1.3

.*Technical Tasks:*
1. Add move-up/move-down buttons to ImageEditor thumbnails
2. Track imageIdsOrder state in EntryForm
3. Extend updateEntry to accept imageIdsOrder: string[]
4. On save: update entry.imageIds order and ImageAttachment.order fields in IndexedDB

.*Estimated Effort:*
3 hours

'''

=== Story 4.7: Image Management in Edit Mode – Replace Image
*As a* user +
*I want to* replace an existing image with a new one +
*So that* I can update a photo without changing the entry structure

.*Acceptance Criteria:*
- Replace button on each existing image thumbnail in edit mode
- Opens file picker for a single image
- Old image is deleted from IndexedDB, new image is compressed and stored
- Entry's imageIds is updated: old ID replaced with new ID at the same position
- Preview updates immediately

.*Reference:*
FR-1.3

.*Technical Tasks:*
1. Add replace button to ImageEditor thumbnails
2. Open file picker (single file mode) on click
3. Validate and compress replacement image
4. Track replacements as map (oldId → SelectedImage) in EntryForm state
5. Extend updateEntry to accept imageReplacements
6. On save: delete old image, store new image, update entry.imageIds

.*Estimated Effort:*
3 hours

'''

=== Story 4.3: Audio Recording
*As a* user +
*I want to* record audio for my entry +
*So that* I can capture the spoken quote

.*Acceptance Criteria:*
- Record button starts MediaRecorder
- Timer shows recording duration
- Stop button saves recording
- Max 5 minutes duration
- Audio stored as Blob in IndexedDB
- Playback available after recording

.*Reference:*
FR-1.4

.*Technical Tasks:*
1. Create hooks/useMediaRecorder.ts
2. Implement startRecording with MediaRecorder API
3. Implement stopRecording
4. Add timer display
5. Enforce 5 minute limit
6. Save audio blob to IndexedDB
7. Add audio player for playback
8. Handle browser compatibility (feature detection)

.*Estimated Effort:*
8 hours

'''

== Epic 5: Author and Label Management

=== Story 5.1: Author Creation
*As a* user +
*I want to* create authors +
*So that* I can attribute quotes

.*Acceptance Criteria:*
- Author form with name (required) and optional fields
- Duplicate name validation
- Save to IndexedDB
- Accessible from entry form

.*Reference:*
FR-2.1, FR-2.2

.*Technical Tasks:*
1. Create components/author/AuthorForm.tsx
2. Implement form validation
3. Create hooks/useAuthors.ts
4. Check for duplicate names (case-insensitive)
5. Save to IndexedDB 'authors' store
6. Integrate into entry form (dropdown)

.*Estimated Effort:*
5 hours

'''

=== Story 5.2: Label Autocomplete
*As a* user +
*I want to* see label suggestions as I type +
*So that* I can reuse existing labels

.*Acceptance Criteria:*
- Suggestions appear after 1 character
- Shows top 10 matches (prefix-based)
- User can select or create new

.*Reference:*
FR-3.2, FR-3.3

.*Technical Tasks:*
1. Create label input component with autocomplete
2. Query IndexedDB for matching labels
3. Display suggestions (datalist or custom dropdown)
4. Allow selection from list
5. Allow creation of new labels
6. Store labels in lowercase

.*Estimated Effort:*
5 hours

'''

== Epic 6: Search and Folders

=== Story 6.1: Text Search
*As a* user +
*I want to* search my entries +
*So that* I can find specific quotes

.*Acceptance Criteria:*
- Search box on home screen
- Debounced search (300ms)
- Case-insensitive partial match
- Searches text, author, labels
- Results displayed as cards

.*Reference:*
FR-4.1

.*Technical Tasks:*
1. Create components/search/SearchBar.tsx
2. Implement debounced search hook
3. Query IndexedDB with text filter
4. Display results in EntryList
5. Highlight matching terms (optional)

.*Estimated Effort:*
6 hours

'''

=== Story 6.2: Smart Folder Creation
*As a* user +
*I want to* create smart folders +
*So that* I can organize my quotes dynamically

.*Acceptance Criteria:*
- Folder form with name and criteria builder
- Criteria: labels, author, date range, location, text
- Save to IndexedDB
- Folders displayed on home screen

.*Reference:*
FR-4.2, FR-4.3

.*Technical Tasks:*
1. Create components/folder/FolderForm.tsx
2. Create components/folder/CriteriaBuilder.tsx
3. Implement criteria state management
4. Save folder to IndexedDB
5. Create hooks/useFolders.ts
6. Query entries matching criteria
7. Display folder cards on home screen

.*Estimated Effort:*
10 hours

'''

== Epic 7: PWA and Offline

=== Story 7.1: Service Worker Setup
*As a* developer +
*I want to* implement a service worker +
*So that* the app works offline

.*Acceptance Criteria:*
- Service worker caches app shell
- Offline functionality for all features
- Install prompt (A2HS)

.*Reference:*
NFR-6

.*Technical Tasks:*
1. Create public/service-worker.js
2. Implement cache-first strategy for assets
3. Implement network-first for data (IndexedDB)
4. Register service worker in main.tsx
5. Create manifest.json
6. Test offline mode
7. Test install to home screen

.*Estimated Effort:*
6 hours

'''

=== Story 7.2: Data Export
*As a* user +
*I want to* export my data +
*So that* I can back it up

.*Acceptance Criteria:*
- Export button in settings
- Downloads JSON file with all data
- Includes media as Base64
- Schema version included

.*Reference:*
NFR-5

.*Technical Tasks:*
1. Create services/export.service.ts
2. Query all data from IndexedDB
3. Convert Blobs to Base64
4. Create JSON structure
5. Trigger download
6. Add export button to UI

.*Estimated Effort:*
5 hours

'''

=== Story 7.3: Data Import
*As a* user +
*I want to* import data +
*So that* I can restore from backup

.*Acceptance Criteria:*
- Import button accepts JSON file
- Validates structure
- Offers merge or replace
- Restores all data including media

.*Reference:*
NFR-5

.*Technical Tasks:*
1. Add file input for import
2. Validate JSON structure
3. Convert Base64 back to Blobs
4. Restore to IndexedDB
5. Handle merge vs replace logic
6. Show progress indicator

.*Estimated Effort:*
5 hours

'''

=== Story 7.4: Export and Import Preserve URL and Citation Metadata
*As a* user +
*I want to* keep citation dates and attached URLs when exporting and importing data +
*So that* backups fully preserve the meaning and provenance of my entries

.*Acceptance Criteria:*
- Exported entry data includes `citationDate` and attached URL metadata
- Imported data restores `citationDate` and attached URL metadata correctly
- Per-URL attachment dates are preserved through export/import
- Import validation rejects malformed URL metadata structures

.*Reference:*
FR-1.7, FR-1.8, NFR-5

.*Technical Tasks:*
1. Extend export schema for enriched `Entry` records
2. Update import validation rules
3. Restore URL metadata and citation dates during import
4. Add regression tests for round-trip export/import

.*Estimated Effort:*
3 hours

'''

== Epic 8: UI Cleanup

=== Story 8.1: Location Text with Reverse Geocoding
*As a* user +
*I want to* see a human-readable place name for each entry's location +
*So that* I can understand where a quote was captured without reading raw coordinates

.*Acceptance Criteria:*
- Location displayed as: `<place name> (lat, lng)` in the entry card
- Place name resolved via Nominatim reverse geocoding
- Cached per entry to avoid repeated API calls
- Graceful fallback to coordinates-only if geocoding fails
- Location icon (📍) shown before the text

.*Reference:*
FR-1.2

.*Technical Tasks:*
1. Add reverse geocoding call in EntryCard for entries with location
2. Cache resolved addresses in component state
3. Display template: `<address> (<coords>)` or just `<coords>` on failure
4. Show a loading placeholder while geocoding

.*Estimated Effort:*
3 hours

'''

=== Story 8.2: MiniMap as Hover Popup
*As a* user +
*I want to* see a map preview when I hover over the location text +
*So that* I get visual context without cluttering the entry card

.*Acceptance Criteria:*
- MiniMap is no longer rendered inline in the entry card
- Hovering over the location text shows a floating MiniMap popup
- Popup appears after a short delay (~300ms) to avoid flicker
- Popup disappears when mouse leaves the location area
- Touch devices: tap toggles popup

.*Reference:*
FR-1.2

.*Technical Tasks:*
1. Create LocationPopover wrapper component
2. Manage hover state with enter/leave + delay
3. Render MiniMap inside an absolutely-positioned popup container
4. Add CSS for popup positioning, shadow, animation
5. Remove inline MiniMap from EntryCard
6. Wrap location text in LocationPopover

.*Estimated Effort:*
4 hours

'''

=== Story 8.3: Click Location to Open Map Viewer
*As a* user +
*I want to* click the location text to open the full map view +
*So that* I can explore the location in detail

.*Acceptance Criteria:*
- Clicking the location text opens the LocationViewer modal
- Same behavior as the current MiniMap click
- Cursor changes to pointer on the location text
- Visual affordance (hover underline) indicating clickability
- Keyboard accessible (Enter/Space)

.*Reference:*
FR-1.2

.*Technical Tasks:*
1. Make location text element clickable with button semantics
2. Wire onClick to existing onLocationClick prop
3. Add CSS for clickable appearance
4. Ensure keyboard accessibility

.*Estimated Effort:*
2 hours

'''

=== Story 8.4: Keyboard, Mouse and Touch Selection for Autocomplete Lists
*As a* user +
*I want to* select entries from the suggestion/oracle lists in labels and authors using keyboard cursor + Tab/Enter, mouse click, or tap on touch devices +
*So that* I can conveniently reuse existing authors and labels without retyping

.*Acceptance Criteria:*
- AuthorSelect is replaced with an autocomplete combobox (text input with dropdown suggestions)
- Typing in the author field filters existing authors by prefix match
- A "Create new author" option appears when the typed text doesn't match an existing author exactly
- Arrow keys (↑/↓) navigate through the suggestion list with visual highlight
- Tab or Enter selects the highlighted suggestion
- Escape closes the dropdown without selecting
- Mouse click on a suggestion selects it
- Touch tap on a suggestion selects it on mobile devices
- LabelInput suggestions support Arrow key navigation (↑/↓) with visual highlight
- Tab or Enter in LabelInput selects the highlighted suggestion
- Both components follow WAI-ARIA combobox pattern (role, aria-activedescendant, aria-expanded)
- Existing "No author" / clear functionality is preserved
- Existing "Create new author" inline flow is preserved

.*Reference:*
FR-2.1, FR-2.2

.*Technical Tasks:*
1. Add `searchAuthors` method to useAuthors hook
2. Rewrite AuthorSelect as autocomplete combobox with text input and dropdown
3. Add keyboard navigation (Arrow keys, Tab, Enter, Escape) to AuthorSelect
4. Add ARIA attributes for accessibility (combobox pattern)
5. Add keyboard navigation (Arrow keys, Tab, Enter) to LabelInput suggestions
6. Add highlighted-item tracking state to both components
7. Update CSS for highlight styles and dropdown appearance
8. Write/update tests for keyboard and mouse interaction

.*Estimated Effort:*
6 hours

'''

=== Story 8.5: URL Dropdown with Favicons on Entry Cards
*As a* user +
*I want to* access attached URLs from a compact icon on the entry card +
*So that* I can open source links without cluttering the quote text

.*Acceptance Criteria:*
- Entry cards with attached URLs show a dedicated link icon in the top-right area of the text block
- Activating the icon opens a dropdown or popover with all attached URLs
- Each URL row shows the resolved favicon when available
- A default favicon is shown when favicon loading fails
- Selecting a URL opens it in the browser
- The dropdown or popover is accessible by mouse, keyboard, and touch

.*Reference:*
FR-5.6

.*Technical Tasks:*
1. Add link icon button to `EntryCard`
2. Create URL dropdown/popover component
3. Resolve favicon from URL origin (`/favicon.ico`)
4. Add fallback favicon asset and loading failure handling
5. Open selected URL with safe browser navigation (`noopener`, `noreferrer`)
6. Add accessibility semantics and tests

.*Estimated Effort:*
5 hours

'''

=== Story 8.6: Citation Date Display in Entry Lists and Cards
*As a* user +
*I want to* see the citation date instead of only the entry creation date +
*So that* the UI reflects when the quote belongs historically

.*Acceptance Criteria:*
- Entry cards display `citationDate` where the quote date is shown
- Search and folder result cards display `citationDate` consistently
- Legacy entries without an explicit citation date fall back to their creation date
- Existing compact author/date line formatting remains intact

.*Reference:*
FR-1.8

.*Technical Tasks:*
1. Update entry date formatting helpers to prefer `citationDate`
2. Update `EntryCard` and related list views
3. Add fallback behavior for legacy entries
4. Add tests for display and fallback behavior

.*Estimated Effort:*
3 hours

'''

== Epic 9: Server Synchronization

=== Story 9.1: Migrate Data Layer from idb to PouchDB [DONE]
*As a* developer +
*I want to* replace the raw `idb` wrapper with PouchDB +
*So that* the data layer supports replication to CouchDB

.*Status:* ✅ Implemented (2026-04-09)

.*Acceptance Criteria:*
- [x] PouchDB is installed and configured as the local database
- [x] `DBService` is rewritten to use PouchDB API (`put`, `get`, `allDocs`, `query`)
- [x] All document types use the `type` field convention (e.g., `type: 'entry'`)
- [x] Document IDs follow the `<type>:<uuid>` pattern
- [x] All existing hooks (`useEntries`, `useAuthors`, `useLabels`, `useFolders`) work with the new DBService
- [x] The `idb` dependency is removed from `package.json`
- [x] Design documents are created for required secondary indexes (createdAt, authorId, entryId, name)
- [x] All existing unit and integration tests pass against the PouchDB-based DBService (267/267)

.*Implementation Notes:*
- `db/schema.ts` rewritten: defines `STORES` as type prefixes (`entry`, `author`, etc.), `buildId()`, `extractUuid()`, and design documents for map/reduce views
- `db.service.ts` rewritten: PouchDB-backed singleton with compatible public API (`get`, `getAll`, `add`, `update`, `delete`, `query`, `clear`, `count`, `getAllEntriesSorted`)
- Documents stored with `_id = <type>:<uuid>`, `type` field, and model fields (minus `id`)
- `toModel()` / `toDoc()` helpers handle conversion between PouchDB docs and public model shapes
- `export.service.ts` updated to use `STORES` constants instead of hardcoded string store names
- `vitest.config.ts` updated: `fileParallelism: false` to prevent cross-file PouchDB state interference
- Hook tests use `dbService.clear()` for fast atomic cleanup
- `db/schema.ts` kept (not removed) — it now defines the PouchDB document conventions and design documents

.*Reference:*
FR-6.2, NFR-2

.*Technical Tasks:*
1. Install `pouchdb-browser` and `pouchdb-find`
2. Rewrite `db.service.ts` to wrap PouchDB instead of `idb`
3. Add `type` field to all document interfaces in `models/`
4. Change document ID generation to `<type>:<uuid>` pattern
5. Create design documents for secondary indexes
6. Update all hooks to use new DBService API
7. Remove `db/schema.ts` (PouchDB is schemaless)
8. Remove `idb` dependency
9. Update all existing tests

.*Test Criteria:*
- *Unit tests:* DBService CRUD methods, query methods, design document creation
- *Integration tests:* All hooks with real PouchDB (using `pouchdb-adapter-memory` for tests)
- *Component tests:* Verify existing component tests still pass with mocked PouchDB DBService

.*Estimated Effort:*
16 hours

'''

=== Story 9.2: Migrate Binary Attachments to PouchDB Attachments [DONE]
*As a* developer +
*I want to* store images and audio as PouchDB attachments instead of separate object store records +
*So that* binary data replicates correctly to CouchDB

.*Status:* ✅ Implemented (2026-04-09)

.*Acceptance Criteria:*
- [x] Images are stored as PouchDB attachments on their parent entry document
- [x] Audio recordings are stored as PouchDB attachments on their parent entry document
- [x] `imageIds` and `audioId` on Entry are replaced by `imageAttachments: ImageAttachmentMeta[]` and `audioAttachment?: AudioAttachmentMeta`
- [x] Image and audio retrieval uses `db.getAttachment()`
- [x] Upload, deletion, and replacement of images work with the attachment API
- [x] Export/import handles PouchDB attachments correctly (Base64 serialization)
- [x] Automatic v2→v3 migration converts legacy separate documents to attachments

.*Implementation Notes:*
- `Entry` model: `imageIds: string[]` → `imageAttachments: ImageAttachmentMeta[]`, `audioId?: string` → `audioAttachment?: AudioAttachmentMeta`
- New model types: `ImageAttachmentMeta`, `ImageAttachmentWithBlob`, `AudioAttachmentMeta`, `AudioAttachmentWithBlob`
- `DBService`: new methods `putAttachment()`, `getAttachment()`, `removeAttachment()`; `update()` preserves `_attachments` stubs
- `db/schema.ts`: `DB_VERSION` bumped to 3; `STORES.IMAGES`/`STORES.AUDIO` removed; design docs for `images`/`audio` removed; `LEGACY_STORES` added for migration
- `useEntries`: `saveImages` stores blobs as PouchDB attachments; `getImagesForEntry` loads from attachments; `deleteEntry` simplified (PouchDB auto-removes attachments)
- `export.service.ts`: exports read attachments from entry documents; imports restore as `putAttachment()`
- All image components updated to use `ImageAttachmentWithBlob` (runtime type with blob)
- Migration: automatic in-place v2→v3 migration on first launch after upgrade

.*Reference:*
FR-6.2, FR-1.3, FR-1.4

.*Technical Tasks:*
1. Update `saveImages` to use `db.putAttachment()` on the entry document
2. Update `getImagesForEntry` to use `db.getAttachment()`
3. Update image deletion and replacement to use attachment API
4. Update audio recording storage to use attachment API
5. Remove separate `images` and `audio` document types
6. Update `export.service.ts` to export attachments
7. Update `import.service.ts` to restore attachments
8. Update `ImageEditor`, `ImageUpload` components

.*Test Criteria:*
- *Unit tests:* Attachment save, retrieve, delete via DBService
- *Integration tests:* Full entry CRUD with attachments, export/import round-trip
- *Component tests:* ImageEditor, ImageUpload with mocked attachment API

.*Estimated Effort:*
10 hours

'''

=== Story 9.3: One-Time Data Migration from Legacy IndexedDB [DONE]
*As a* user +
*I want to* have my existing data automatically migrated to the new database format +
*So that* I do not lose any data when the app upgrades

.*Status:* ✅ Implemented (2026-04-09)

.*Acceptance Criteria:*
- [x] On first launch after upgrade, the app detects legacy `idb`-based IndexedDB data
- [x] All entries, authors, labels, folders, images, and audio are migrated to PouchDB
- [x] Binary blobs are converted to PouchDB attachments (v3 naming: `image-<uuid>`, `audio-<uuid>`)
- [x] Entry documents receive `imageAttachments` / `audioAttachment` metadata arrays
- [x] Migration runs once and marks itself as complete (flag in localStorage)
- [x] If migration fails, the user sees an error screen with guidance
- [x] Legacy IndexedDB data is preserved (not deleted) after migration

.*Implementation Notes:*
- `services/migration.service.ts` rewritten: typed interfaces for legacy records, 3-phase migration (documents → image attachments → audio attachments), v3-compatible attachment naming
- `App.tsx`: migration check runs on mount with blocking progress UI; `window.location.reload()` after successful migration to reinitialise all hooks
- `test/setup.ts`: localStorage polyfill for Node.js 25 compatibility (Node.js 25 ships a bare `localStorage` without Web Storage API methods)
- Tests: 7 tests covering detection, flag management, entry/author/label/folder migration, image attachment metadata, progress callback, skip-when-already-migrated

.*Reference:*
FR-6.2, NFR-2

.*Technical Tasks:*
1. Create `services/migration.service.ts`
2. Detect legacy `idb` database existence
3. Read all records from legacy object stores
4. Write records to PouchDB with new document format
5. Convert blobs to PouchDB attachments
6. Set `migrationComplete` flag in localStorage
7. Show migration progress indicator to user
8. Handle partial failure with rollback/retry

.*Test Criteria:*
- *Unit tests:* Migration detection, document conversion logic, flag management
- *Integration tests:* Full migration from fake-indexeddb legacy stores to PouchDB in-memory

.*Estimated Effort:*
8 hours

'''

=== Story 9.4: Replace storeSync with PouchDB Change Feed
*As a* developer +
*I want to* use PouchDB's built-in change feed instead of the custom storeSync event emitter +
*So that* hooks receive updates from both local changes and remote replication

.*Acceptance Criteria:*
- `storeSync.ts` is removed
- All hooks subscribe to PouchDB `db.changes({ live: true, since: 'now' })` for real-time updates
- Changes from local writes trigger hook reloads
- Changes from remote replication trigger hook reloads
- Hooks filter change events by document type to avoid unnecessary reloads
- No duplicate reloads when the same hook instance triggers a write

.*Reference:*
FR-6.2

.*Technical Tasks:*
1. Create `useDbChanges` hook that wraps `db.changes()`
2. Update `useEntries`, `useAuthors`, `useLabels`, `useFolders` to use `useDbChanges`
3. Add document type filtering in change listener
4. Remove `storeSync.ts` and all references
5. Verify cross-tab synchronization still works

.*Test Criteria:*
- *Unit tests:* `useDbChanges` hook with mocked PouchDB change feed
- *Integration tests:* Two hook instances receiving each other's changes
- *Component tests:* EntryList re-rendering after external change event

.*Estimated Effort:*
6 hours

'''

=== Story 9.5: CouchDB Docker Setup
*As a* developer +
*I want to* add a CouchDB container to the docker-compose configuration +
*So that* the server-side database is ready for sync

.*Acceptance Criteria:*
- `docker-compose.yml` includes a CouchDB service
- CouchDB is configured with the `couch_peruser` plugin enabled
- Admin credentials are configurable via environment variables
- CouchDB data is persisted via a Docker volume
- CORS is enabled for the web app origin
- CouchDB health check is configured
- The web app container can reach CouchDB via Docker networking

.*Reference:*
FR-6.2, FR-6.3

.*Technical Tasks:*
1. Add CouchDB service to `docker-compose.yml`
2. Configure `couch_peruser` plugin in CouchDB config
3. Add CORS configuration for web app origin
4. Add Docker volume for CouchDB data persistence
5. Add health check for CouchDB readiness
6. Document CouchDB admin setup in README

.*Test Criteria:*
- *Blackbox test:* Docker compose up starts both containers, CouchDB responds on health endpoint

.*Estimated Effort:*
4 hours

'''

=== Story 9.6: Authentication Service
*As a* user +
*I want to* register and log in +
*So that* my data can be synchronized to the server

.*Acceptance Criteria:*
- User can register with email and password
- User can log in with email and password
- User can log out
- Authentication session persists across page reloads (stored in localStorage)
- Invalid credentials show a clear error message
- Registration validates email format and minimum password length (8 characters)
- Authentication state is available to React components via `useAuth` hook

.*Reference:*
FR-6.1

.*Technical Tasks:*
1. Create `services/auth.service.ts` with register, login, logout methods
2. Implement CouchDB `_users` database interaction for registration
3. Implement CouchDB `_session` API for login/logout
4. Store session in localStorage
5. Create `hooks/useAuth.ts` hook exposing auth state and actions
6. Add error handling for network failures, invalid credentials, duplicate email

.*Test Criteria:*
- *Unit tests:* AuthService register, login, logout, session persistence
- *Integration tests:* useAuth hook with mocked AuthService

.*Estimated Effort:*
8 hours

'''

=== Story 9.7: Authentication UI
*As a* user +
*I want to* see login and registration forms in the app +
*So that* I can create an account and sign in

.*Acceptance Criteria:*
- Login/Register button visible in the header when not authenticated
- Clicking the button opens a modal with login and registration tabs
- Login form has email and password fields with validation
- Registration form has email, password, and confirm password fields with validation
- Successful login closes the modal and shows user indicator in header
- Logged-in state shows user email and logout button in header
- All forms are keyboard accessible and responsive

.*Reference:*
FR-6.1

.*Technical Tasks:*
1. Create `components/auth/AuthModal.tsx` with login and register tabs
2. Create `components/auth/LoginForm.tsx`
3. Create `components/auth/RegisterForm.tsx`
4. Add auth button to Header component
5. Add logged-in user display to Header
6. Wire forms to `useAuth` hook
7. Add form validation (email format, password length, password match)
8. Style for mobile and desktop

.*Test Criteria:*
- *Component tests:* LoginForm validation, RegisterForm validation, AuthModal tab switching
- *Component tests:* Header auth button states (logged in vs. logged out)

.*Estimated Effort:*
6 hours

'''

=== Story 9.8: Sync Service Implementation
*As a* user +
*I want to* have my data synchronized to the server automatically after login +
*So that* my data is backed up and accessible from other devices

.*Acceptance Criteria:*
- After login, bidirectional live replication starts between PouchDB and the user's CouchDB database
- Replication pauses when the browser goes offline and resumes when online
- Replication uses retry with exponential backoff
- Sync stops when the user logs out
- New and modified records appear on both ends within seconds
- Binary attachments (images, audio) are included in replication

.*Reference:*
FR-6.2, FR-6.3, NFR-7

.*Technical Tasks:*
1. Create `services/sync.service.ts` with startSync, stopSync, getStatus
2. Implement `PouchDB.sync(localDb, remoteUrl, { live: true, retry: true })`
3. Listen for `navigator.onLine` events to pause/resume
4. Expose sync status via `useSyncStatus` hook
5. Wire sync start/stop to login/logout in `useAuth`
6. Handle authentication errors during sync (re-login prompt)

.*Test Criteria:*
- *Unit tests:* SyncService start, stop, status transitions, offline handling
- *Integration tests:* Bidirectional replication between two PouchDB in-memory instances

.*Estimated Effort:*
8 hours

'''

=== Story 9.9: Sync Status UI
*As a* user +
*I want to* see the current sync status in the app +
*So that* I know whether my data is up to date on the server

.*Acceptance Criteria:*
- Sync status indicator visible in the header when authenticated
- Status displays: idle (✓), syncing (↻), paused/offline (⏸), error (⚠)
- Clicking the indicator shows sync details (last sync time, pending changes count)
- Error status shows a brief error message with retry option

.*Reference:*
FR-6.2

.*Technical Tasks:*
1. Create `components/common/SyncIndicator.tsx`
2. Create `hooks/useSyncStatus.ts` consuming SyncService events
3. Add SyncIndicator to Header (only when authenticated)
4. Style icons for each status state
5. Add tooltip or popover with sync details

.*Test Criteria:*
- *Component tests:* SyncIndicator renders correct icon for each status
- *Component tests:* SyncIndicator shows details on click

.*Estimated Effort:*
4 hours

'''

=== Story 9.10: Conflict Detection and Duplication
*As a* user +
*I want to* see both versions when a conflict occurs during sync +
*So that* I can decide which version to keep

.*Acceptance Criteria:*
- After sync, documents with `_conflicts` are detected automatically
- For each conflict, the losing revision is extracted and saved as a new document
- Both documents are marked with a `conflictSource` field linking to each other
- Conflicting entries are visually marked with a ⚠️ icon in entry lists and cards
- The user can edit or delete either version using existing edit/delete functions
- Resolving a conflict (deleting one version) removes the conflict marker from the other
- Conflicts on authors, labels, and folders are handled with the same strategy

.*Reference:*
FR-6.4

.*Technical Tasks:*
1. Add conflict scanning to `SyncService.resolveConflicts()`
2. Extract losing revisions via `db.get(id, { conflicts: true })`
3. Create duplicate document from losing revision with new ID
4. Add `conflictSource` field to both documents
5. Add `isConflicted` computed property to entry display logic
6. Add ⚠️ indicator to `EntryCard`
7. Clear `conflictSource` when linked document is deleted
8. Handle conflicts for author, label, and folder document types

.*Test Criteria:*
- *Unit tests:* Conflict detection logic, duplicate creation, conflict marker cleanup
- *Integration tests:* Two PouchDB instances writing conflicting changes, sync, verify duplicates created
- *Component tests:* EntryCard displays conflict indicator when `conflictSource` is set

.*Estimated Effort:*
10 hours

'''

=== Story 9.11: Sync Opt-In and Local-Only Mode
*As a* user +
*I want to* use the app without creating an account +
*So that* the app works exactly as before if I do not need sync

.*Acceptance Criteria:*
- The app launches and operates identically for unauthenticated users
- No server requests are made unless the user logs in
- PouchDB operates in local-only mode without replication
- A user who logs out continues using the app with all local data intact
- The sync UI elements (status indicator, auth button) degrade gracefully when not authenticated

.*Reference:*
FR-6.5

.*Technical Tasks:*
1. Guard SyncService.startSync behind authentication check
2. Ensure DBService works without any remote configuration
3. Hide SyncIndicator when not authenticated
4. Show login/register prompt as non-intrusive option (not blocking)
5. Verify all existing functionality works without authentication

.*Test Criteria:*
- *Integration tests:* Full app flow without authentication (no sync attempted)
- *Component tests:* Header renders without sync elements when not authenticated
- *Blackbox test:* Complete user journey (create, edit, search, delete entry) without login

.*Estimated Effort:*
4 hours

'''

== Implementation Phases

=== Phase 1: Foundation (Weeks 1-2)
.*Goal:*
Core data layer and basic entry management

.*Stories:*
- Epic 1: All stories (1.1-1.4)
- Epic 2: Stories 2.1-2.4
- Epic 3: Story 3.1

.*Deliverable:*
Can create and save basic entries with text and location

'''

=== Phase 2: Media and Authors (Weeks 3-4)
.*Goal:*
Complete entry creation with media and authors

.*Stories:*
- Epic 4: All stories (4.1-4.3)
- Epic 5: All stories (5.1-5.2)
- Epic 2: Stories 2.5-2.6
- Epic 3: Stories 3.2-3.3

.*Deliverable:*
Full-featured entry creation with images, audio, authors, labels

'''

=== Phase 3: Search and Organization (Weeks 5-6)
.*Goal:*
Search and smart folders

.*Stories:*
- Epic 6: All stories (6.1-6.2)

.*Deliverable:*
Working search and smart folder system

'''

=== Phase 4: PWA and Polish (Week 7)
.*Goal:*
Offline capability and data portability

.*Stories:*
- Epic 7: All stories (7.1-7.3)
- UI polish and bug fixes

.*Deliverable:*
Production-ready PWA with offline support and export/import

'''

=== Phase 5: Server Sync (Weeks 8-11)
.*Goal:*
PouchDB migration, CouchDB sync, user authentication, conflict resolution

.*Stories:*
- Epic 9: Stories 9.1-9.4 (PouchDB migration, attachments, legacy migration, change feed)
- Epic 9: Story 9.5 (CouchDB Docker setup)
- Epic 9: Stories 9.6-9.7 (Authentication service and UI)
- Epic 9: Stories 9.8-9.9 (Sync service and status UI)
- Epic 9: Stories 9.10-9.11 (Conflict resolution and local-only mode)

.*Deliverable:*
Multi-device sync with offline-first architecture and conflict resolution

'''

== Definition of Done

A story is considered "Done" when:

1. Code is written and passes linting
2. TypeScript type-safe (no 'any' types)
3. Unit tests written and passing (where applicable)
4. Component tests for UI components
5. Tested in Chrome, Firefox, Safari
6. Tested on mobile viewport (320px)
7. Accessibility verified (keyboard nav, screen reader)
8. Dark mode tested
9. Code reviewed
10. Merged to main branch

'''

== Technical Debt Tracking

=== Known Compromises for v1.0

1. **LLM Features:** Deferred to v2.0 (FE-1, FE-2)
2. **Cloud Sync:** Promoted to Epic 9 (PouchDB + CouchDB), planned for Phase 5
3. **Advanced Media Editing:** No cropping/filters in v1.0
4. **Social Features:** Deferred to v2.0 (FE-5)
5. **Multi-language:** Deferred to v2.0

These will be prioritized based on user feedback after v1.0 launch.

'''

*Total Estimated Effort:* ~204 hours (~120h existing + ~84h Epic 9)
*Target Completion:* 11 weeks from start
