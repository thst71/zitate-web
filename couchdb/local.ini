; CouchDB local configuration for Zitate
; This file is COPY'd into the CouchDB image at /opt/couchdb/etc/local.d/zitate.ini

[couchdb]
; No custom couchdb-section settings needed; defaults are fine.

[cluster]
; Single-node setup (no clustering)
n = 1

[couch_peruser]
; Automatically create a database for each new user
enable = false
; Database names follow the pattern: userdb-<hex-encoded-username>
delete_dbs = false

[chttpd]
; Bind to all interfaces inside the container
bind_address = 0.0.0.0
port = 5984
; Require authentication for all requests except /_up (health check)
require_valid_user_except_for_up = true

[httpd]
enable_cors = true

[cors]
; Origins allowed to make requests to CouchDB.
; In production behind nginx reverse proxy, requests arrive from the same
; origin so CORS headers are technically unnecessary. We enable them for
; direct-access development scenarios (e.g. Vite dev server on localhost:5173).
origins = *
credentials = true
methods = GET, PUT, POST, HEAD, DELETE, OPTIONS
headers = accept, authorization, content-type, origin, referer, x-csrf-token

[log]
level = info

[admins]
admin = -pbkdf2:sha256-33079895712ceee087be7c0d8bad7864e8a1fb29a09b79b98eb7f489e3e3bf97,99bbe81533042153f555dee56d7bd2a3,600000
